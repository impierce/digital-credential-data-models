[package]
name = "openbadges"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
regress = "0.7"
chrono = { version = "0.4", features = ["serde"] }
